import{notFound}from'next/navigation';import DetailClient from'../../../ui/DetailClient';import{getDetail}from'../../../lib/tmdb';export default async function Page({params}:{params:Promise<{type:string;id:string}>}){const{type,id}=await params;if(type!=='movie'&&type!=='tv')return notFound();const d=await getDetail(type,id);if(!d)return notFound();return <DetailClient detail={d}/>}
