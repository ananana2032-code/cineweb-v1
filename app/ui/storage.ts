"use client";import type{Media}from'../lib/tmdb';const F='cineweb:favorites',H='cineweb:history';function read(k:string):Media[]{if(typeof window==='undefined')return[];try{return JSON.parse(localStorage.getItem(k)||'[]')}catch{return[]}}function write(k:string,v:Media[]){localStorage.setItem(k,JSON.stringify(v));window.dispatchEvent(new Event('cineweb-storage'))}export const favorites=()=>read(F);export const history=()=>read(H);export const isFavorite=(m:Media)=>favorites().some(x=>x.id===m.id&&x.type===m.type);export function toggleFavorite(m:Media){const a=favorites(),e=isFavorite(m);write(F,e?a.filter(x=>!(x.id===m.id&&x.type===m.type)):[m,...a]);return!e}export function addHistory(m:Media){write(H,[m,...read(H).filter(x=>!(x.id===m.id&&x.type===m.type))].slice(0,20))}
