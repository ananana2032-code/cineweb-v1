"use client";import Link from'next/link';import{Film,Star}from'lucide-react';import type{Media}from'../lib/tmdb';export default function MediaCard({item}:{item:Media}){return <Link className="card" href={`/title/${item.type}/${item.id}`}><div className="poster">{item.poster?<img src={item.poster} alt={item.title}/>:<div className="placeholder"><Film size={32}/></div>}<span className="score"><Star size={13} fill="currentColor"/> {item.rating.toFixed(1)}</span></div><div className="cardTitle">{item.title}</div><div className="meta">{item.year||'—'} · {item.type==='tv'?'Série':'Filme'}</div></Link>}
