"use client";import Header from'./Header';import MediaCard from'./MediaCard';import type{Media}from'../lib/tmdb';export default function SearchPage({query,results}:{query:string;results:Media[]}){return <main><Header/><div className="page"><h1>{query?`Resultados para “${query}”`:'Buscar'}</h1><p className="muted">{results.length} resultado(s)</p><div className="grid">{results.map(i=><MediaCard item={i} key={`${i.type}-${i.id}`}/>)}</div></div></main>}
