"use client";import Link from'next/link';import{Play,Plus,Star}from'lucide-react';import{useEffect,useState}from'react';import Header from'./Header';import MediaCard from'./MediaCard';import type{Media}from'../lib/tmdb';import{history,toggleFavorite}from'./storage';type P={data:{hero:Media;trending:Media[];movies:Media[];tv:Media[];top:Media[];demo:boolean}};function Rail({title,items}:{title:string;items:Media[]}){return <section className="rail"><div className="sectionTitle"><h2>{title}</h2></div><div className="row">{items.map(i=><MediaCard item={i} key={`${i.type}-${i.id}`}/>)}</div></section>}export default function HomeClient({data}:P){const[recent,setRecent]=useState<Media[]>([]);useEffect(()=>{const l=()=>setRecent(history());l();window.addEventListener('cineweb-storage',l);return()=>window.removeEventListener('cineweb-storage',l)},[]);const h=data.hero;return <main><Header/><section className="hero" style={h?.backdrop?{backgroundImage:`linear-gradient(90deg,#08090d 2%,rgba(8,9,13,.82) 38%,rgba(8,9,13,.18) 76%,#08090d 100%),linear-gradient(0deg,#08090d 0%,transparent 45%),url(${h.backdrop})`}:{}}><div className="heroContent"><span className="eyebrow">DESTAQUE DA SEMANA</span><h1>{h.title}</h1><div className="heroMeta"><b>{h.year}</b><span><Star size={15} fill="currentColor"/> {h.rating.toFixed(1)}</span><span>{h.type==='tv'?'Série':'Filme'}</span></div><p>{h.overview}</p><div className="actions"><Link className="primary" href={`/title/${h.type}/${h.id}`}><Play size={19} fill="currentColor"/>Ver detalhes</Link><button className="secondary" onClick={()=>toggleFavorite(h)}><Plus size={20}/>Minha lista</button></div></div></section>{data.demo&&<div className="demoBanner">Modo demonstração. Configure <b>TMDB_READ_TOKEN</b> na Vercel.</div>}{recent.length>0&&<Rail title="Vistos recentemente" items={recent}/>}<Rail title="Em alta" items={data.trending}/><Rail title="Filmes em cartaz" items={data.movies}/><Rail title="Séries populares" items={data.tv}/><Rail title="Mais bem avaliados" items={data.top}/><footer className="footer">Este produto usa a API do TMDB, mas não é endossado nem certificado pelo TMDB.</footer></main>}
